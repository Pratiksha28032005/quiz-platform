import React, { useEffect, useState } from 'react';
import api from '../api/axios';

export default function Leaderboard() {
  const [rows, setRows] = useState([]);
  useEffect(() => { api.get('/leaderboard').then((res) => setRows(res.data)); }, []);
  return <main className="page-narrow"><div style={{ marginBottom: 28 }}><p className="eyebrow">A little friendly competition</p><h1 className="display-title" style={{ fontSize: 'clamp(2rem, 4vw, 3rem)' }}>The leaderboard</h1><p className="muted" style={{ margin: '10px 0 0' }}>Consistency gets you everywhere. See who’s on a roll.</p></div><section className="panel" style={{ padding: 0, overflow: 'hidden' }}><div style={{ display: 'grid', gridTemplateColumns: '60px 1fr 120px 90px', gap: 10, padding: '14px 22px', color: '#9295ac', fontSize: '.7rem', fontWeight: 800, textTransform: 'uppercase', letterSpacing: '.06em', background: '#fafaff' }}><span>#</span><span>Student</span><span>Avg. score</span><span>Done</span></div>{rows.map((r, idx) => <div key={r.user_id} style={{ display: 'grid', gridTemplateColumns: '60px 1fr 120px 90px', gap: 10, alignItems: 'center', padding: '17px 22px', borderTop: '1px solid #f0f0f6' }}><strong style={{ color: idx < 3 ? 'var(--coral)' : 'var(--muted)' }}>{String(idx + 1).padStart(2, '0')}</strong><span style={{ display: 'flex', alignItems: 'center', gap: 10, fontWeight: 700, fontSize: '.86rem' }}><span className="avatar" style={{ width: 30, height: 30, fontSize: '.72rem' }}>{r.name?.charAt(0).toUpperCase()}</span>{r.name}</span><strong style={{ color: 'var(--brand)', fontSize: '.86rem' }}>{r.average_score}%</strong><span className="muted" style={{ fontSize: '.8rem' }}>{r.quizzes_completed}</span></div>)}{rows.length === 0 && <div className="empty-state" style={{ margin: 20 }}>No scores yet. Be the first name on the board.</div>}</section></main>;
}
